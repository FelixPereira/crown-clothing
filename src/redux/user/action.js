const setCurrentUser = user => ({
    type: 'SET_CURRENT_USER',
    payload: user
  })

  const setCurrentUse = user => {
    return(
      {
        type: 'SET_CURRENT_USER',
        payload: user
      }
    )
  }

